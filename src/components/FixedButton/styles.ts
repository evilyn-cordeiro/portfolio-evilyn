import { Button } from "@mui/material";
import { styled } from "@mui/system";

export const StyledButton = styled(Button)({
  position: "fixed",
  bottom: "20px",
  right: "20px",
  zIndex: 1000,
  borderRadius: "100%",
  width: "60px",
  height: "60px",
});
